models:
  Certificate:
    name: string
    reference: string
    document: string
    expiry_date: date
    remarks: text nullable
    relationships:
      belongsTo: \App\Certificate
      hasMany: \App\Certificate

controllers:
  Api/Certificate:
    resource: api
