models:
  User:
    email: string
    relationships:
      belongsToMany: Team:&Membership

  Team:
    name: string
    relationships:
      belongsToMany: User:&Membership

  Membership:
    meta:
      pivot: true
      table: another_table_name
    user_id: id
    team_id: id
