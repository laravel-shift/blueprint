controllers:
  User:
    index:
      query: all:users
      render: user.index with:users

    store:
      validate: name
      save: user
      flash: user.name
      redirect: post.index

config:
  app_path: shift
  namespace: Some\App
  controllers_namespace: Other\Http
